<turbo-stream action="replace" target="place_details">
  <template>
    <div id="place_details" class="flex w-full h-full">
      <div class="flex flex-col w-full p-5">
        <div class="mb-3">
          <span class="bg-yellow-100 p-2 text-xs md:text-lg">📍 Your Stop: <span class="font-bold"><%= @place.name %></span></span>
        </div>
        <div class="grow w-full relative border-dashed border-2 rounded-lg">
          <iframe id="street_view_iframe" src="<%= @place.iframes.first %>" width="100%" height="100%" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
        </div>
      </div>
      <%= render partial:"sidenav", formats: :html, locals: { active_index: 1 } %>
    </div>
  </template>
</turbo-stream>
